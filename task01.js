/*'use strict'

const randomizer = (num) => {
    const arr = [];
    for ( let i = 0; i < num;i++) {
        arr.push(Math.round(Math.random() * 100));
    }
    return arr;
}
console.log(randomizer(20));
*/