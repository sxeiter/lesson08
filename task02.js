/*'use strict';

const randomizer = ( num , n , m ) => {
     const arr = [];
     for ( let i = 0; i < num;i++) {
         arr.push(Math.round(Math.random() * (n - m) + m));
     }
     return arr;
}
 console.log(randomizer(20, 5, -7));
 */